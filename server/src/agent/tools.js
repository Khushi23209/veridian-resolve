import {
  getPolicies,
  searchPolicies
} from "../services/policyService.js";

import {
  getTickets,
  searchTickets
} from "../services/ticketService.js";

export const tools = {
  getPolicies,
  searchPolicies,
  getTickets,
  searchTickets
};
